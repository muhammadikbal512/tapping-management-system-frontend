import { HeaderConfiguration } from './header-configuration.model';

describe('HeaderConfiguration', () => {
  it('should create an instance', () => {
    expect(new HeaderConfiguration()).toBeTruthy();
  });
});
