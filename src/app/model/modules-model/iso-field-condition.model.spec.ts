import { IsoFieldCondition } from './iso-field-condition.model';

describe('IsoFieldCondition', () => {
  it('should create an instance', () => {
    expect(new IsoFieldCondition()).toBeTruthy();
  });
});
